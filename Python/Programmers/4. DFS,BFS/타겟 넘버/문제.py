def solution(numbers, target):
    count = 0
    while(True):
        while(index == numbers[-1])
        if(sum == target):
            count += 1

    return count


numbers = [1, 1, 1, 1, 1]
print(solution(numbers, 3))
